## install pnpm

`curl -fsSL https://get.pnpm.io/install.sh | sh -`

## launch local

`cd frontend`
`pnpm install`
`pnpm run dev`

## docker

`docker build -f Dockerfile -t example`

`docker run example`
